# Historical content

These data files belonged to commented-out homepage sections. They are kept for
reference, but Jekyll does not load or publish this directory. The active site's
data lives in `_data/`.

The text and links here have not been updated. Review them before reusing them.
